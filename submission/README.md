# OnTrack upload (code slot)

Upload this file to the first **Report** slot (code / `</>` icon):

- `PackageRegistry.txt` — full Solidity source (same as `contracts/PackageRegistry.sol`; `.txt` because OnTrack does not accept `.sol` or `.zip`)

Upload your written report (PDF/Word) to the second **Report** slot (document icon).

Full DApp source (contract + React frontend + tests):  
https://github.com/sunnieho0323/npm-blockchain-project
